//your JS code here. If required.
let info = document.getElementById("browser-info");
let b_name=navigator.appName
let version=navigator.appVersion


info.append("You are using "+b_name+" version "+version);